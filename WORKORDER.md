# WORK ORDER — dependency graph across TODO items

Generated by hand 2026-08-29, kept beside `TODO.md`. An item may not
start before its blockers are green. Items on the same tier are
independent and may proceed in any order or in parallel.

## Tier 0 — roots. Nothing depends on anything.

- **[8] hook surface.**
  builds: `check_spec.py`, `settings.json`
 The container ships no `.claude/settings.json`,
  so there is no interception layer at all. **This blocks every
  enforcement mechanism in the tree** — 7.8's interception, item 2's
  itch capture, item 6's ticker, and the spec-check hook. Root of the
  whole enforcement chain and currently a footnote.
- **[5] pin-parity port.**
  builds: `check_pin_parity.py`
 Standalone, built, unwired. Needs only the
  operator's ruling on restore-vs-amend.
- **[1] glossary.** Drafted (6 entries). Needs only placement.

## Tier 1 — needs Tier 0

- **[2] itch protocol** ← needs 8 (capture has to fire from somewhere)
- **[7] structured emission** ← needs 8 (7.8's interception is a hook)
- **[9→11] MAP — blueprint. DONE 2026-08-29 (TODO § 11).** ← needs the corpus inventory, which is
  **partially done**: paper coverage stops at entry 165 (measured),
  results envelope conformance is 66–95% by field (measured), five
  local link conventions exist and none join (read). **The map has no
  blueprint. Writing it is the next artifact, not more code.**

## Tier 2 — needs Tier 1

- **[3] trap graduation** ← needs 2 (dispositions need a capture point)
- **[6] itch ticker** ← needs 2 and at least two detectors; candidates
  are 5's parity check and 10's orphan class
- **[11] MAP — build** ← blueprint done; S1 and S2 unblocked, S4 blocked on the operator
  builds: `map_nodes.py`, `map_edges.py`, `map_render.py`

## Tier 3

- **[10] unambiguous corpus** ← needs S0 inventory complete, and its
  five § 10.4 questions answered
- **[4] workflow chart** ← this document is its seed; it should replace
  this by hand-maintenance being unnecessary

## Adjacency rule

When a tier-N item blocks, work an item on the same tier rather than
skipping ahead. Skipping ahead is what produced `mapgraph.py` — code for
a thing with no blueprint.
